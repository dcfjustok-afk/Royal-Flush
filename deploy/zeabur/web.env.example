# Selects the root-level Dockerfile.web. Use the suffix only.
ZBPACK_DOCKERFILE_NAME=web

# SERVER_HOST is injected from the Zeabur service named exactly "server".
API_UPSTREAM=http://${SERVER_HOST}:8080
